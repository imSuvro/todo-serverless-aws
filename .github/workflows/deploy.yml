name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI/CD pipeline not yet configured. Deploy manually via serverless deploy."
